# Trak — Android Play Store Preparation: Steps 1–6 Report

**Date:** August 23, 2026
**Scope:** Steps 1–6 ONLY. Steps 7–9 (EAS credentials, AAB build, Play Console upload) were **NOT** performed.

---

## 1. Expo SDK Version

- Expo SDK **54** (`expo ~54.0.37`, resolved `sdkVersion: 54.0.0`)
- Expo CLI 54.0.27 · EAS CLI 22.0.0
- Project type: **managed workflow** with a gitignored local `android/` directory (regenerated by EAS prebuild server-side). No `app.config.js/ts` override exists — `app.json` is the single config source.

## 2. React Native Version

- React Native **0.81.5** · React 19.1.0 · Hermes enabled · New Architecture enabled

## 3. Android API / Target Configuration

- **Before:** Expo SDK 54 Gradle defaults → `compileSdk 35 / targetSdk 35` (verified in `expo-modules-autolinking` Gradle plugin source; no override existed). Google Play requires **API 36 from Aug 31, 2026**.
- **Fix applied (Step 1):** installed `expo-build-properties@~1.0.10` (SDK-54-matched via `npx expo install`) and configured it in `app.json`:

```json
["expo-build-properties", {
  "android": {
    "compileSdkVersion": 36,
    "targetSdkVersion": 36,
    "buildToolsVersion": "36.0.0"
  }
}]
```

- No native files were hand-edited (correct for managed/EAS builds — the plugin applies at prebuild time).
- No Expo SDK / React Native upgrade was needed or performed.
- Verified: `npx expo config --type public` resolves the plugin with API 36 values.
- Note: RN 0.81 is Android-16-ready and the project already runs edge-to-edge (`edgeToEdgeEnabled=true`), so no code changes were required. Predictive back becomes OS-enforced at API 36 (`predictiveBackGestureEnabled: false` will be ignored) — expo-router already handles back correctly; smoke-test after first build.

## 4. Icon Audit Results (before fix)

| Asset | Before | Problem |
|---|---|---|
| assets/icon.png | 485×505 | Non-square (expo-doctor schema failure) |
| assets/adaptive-icon.png | 485×505 | Non-square |
| assets/android-icon-foreground.png | 485×505 | Non-square; opaque (no transparency) |
| assets/android-icon-monochrome.png | 485×505 | Non-square; colored, not monochrome |
| assets/android-icon-background.png | 485×505 | Contained the logo instead of a flat background |

All five were the same Trak arrow logo (transparent-background glyph).

## 5. Final App Icon Dimensions

All regenerated **1024×1024 PNG**, logo uniformly scaled and centered (no stretch, no crop, branding preserved):

| Asset | After | Content |
|---|---|---|
| icon.png | 1024×1024 | Trak logo on brand navy `#071B2B` |
| adaptive-icon.png | 1024×1024 | Same composite (iOS) |
| android-icon-foreground.png | 1024×1024 | Colored glyph, transparent bg, within 66% adaptive safe zone |
| android-icon-monochrome.png | 1024×1024 | Pure-white glyph, transparent bg (verified: 100% white pixels) |
| android-icon-background.png | 1024×1024 | Solid brand navy `#071B2B` |
| notification-icon.png (new) | 1024×1024 | Pure-white glyph, transparent bg |

## 6. Adaptive Icon Configuration

`app.json` unchanged in structure (still valid): `adaptiveIcon.backgroundColor: "#071B2B"`, `foregroundImage: ./assets/android-icon-foreground.png`, `monochromeImage: ./assets/android-icon-monochrome.png`. The foreground/monochrome assets are now properly transparent/white, matching the navy `backgroundColor`.

## 7. Monochrome Notification Icon Configuration

- New asset `assets/notification-icon.png` (white glyph on transparent, square).
- `expo-notifications` plugin updated: `"icon": "./assets/notification-icon.png"` (was `./assets/icon.png`), `"color": "#39FF88"` unchanged.
- Notification behavior, permissions, and Supabase/push logic were **not** modified.

## 8. expo-doctor Result

- Before Steps 1–3: **16/18** (icon schema failure + duplicate react-dom).
- After: **17/18**.
  - ✅ Fixed (related to Steps 1–3): "Check Expo config schema" — icon dimension errors resolved.
  - ⚠️ Remaining (pre-existing, unrelated to Steps 1–3, documented not fixed per instructions): duplicate `react-dom` (19.1.0 root vs 18.3.1 nested under **unused** `expo-head@^0.0.20`). Web-only dependency; does not affect Android native builds. Safe future fix: remove `expo-head` from package.json (zero imports in code).

## 9. TypeScript Result

`npx tsc --noEmit` → **0 errors** (exit code 0), run after all changes.

## 10. Files Modified

- `app.json` — expo-build-properties API 36 plugin + notification icon path
- `package.json` — added `expo-build-properties ~1.0.10`
- `package-lock.json` — lockfile update for the above
- `assets/icon.png`, `assets/adaptive-icon.png`, `assets/android-icon-foreground.png`, `assets/android-icon-monochrome.png`, `assets/android-icon-background.png` — regenerated square 1024×1024

## 11. Files Created

- `assets/notification-icon.png` (1024×1024 monochrome white)

## 12. Files Deleted

None.

## 13. Git Diff Summary (commit b729de6)

```
app.json                          |  12 ++++++++-
assets/adaptive-icon.png          |  Bin 194434 -> 587695 bytes
assets/android-icon-background.png|  Bin 194434 -> 6539 bytes
assets/android-icon-foreground.png|  Bin 194434 -> 410310 bytes
assets/android-icon-monochrome.png|  Bin 194434 -> 118413 bytes
assets/icon.png                   |  Bin 194434 -> 587695 bytes
assets/notification-icon.png      |  Bin 0 -> 118413 bytes (new)
package-lock.json                 |  52 ++++++++++
package.json                      |   1 +
9 files changed, 64 insertions(+), 1 deletion(-)
```

## 14. Commit Hash

`b729de6`

## 15. Commit Message

`prepare Android app for Play Store`

## 16. Confirmation: No EAS Build Performed

✅ Confirmed. No `eas build`, no `eas login`, no `eas credentials`, no keystore creation, no AAB/APK generated.

## 17. Confirmation: No Play Console Upload Performed

✅ Confirmed. Nothing submitted or uploaded to Google Play Console.

## 18. Remaining Issues

1. **Pre-existing doctor warning (documented, not fixed):** duplicate `react-dom` via unused `expo-head`. Recommended future action: `npm uninstall expo-head`.
2. **Pre-existing uncommitted changes (NOT committed, per instructions):**
   - `app/settings.tsx` (privacy-policy link + Delete Account entry from earlier sessions)
   - `app/terms.tsx`, `components/legal/LegalDocument.tsx`, `constants/config.ts`
   - Untracked: `public/index.html`, `FIREBASE_ROOT_PAGE_FIX_REPORT.md`, `PLAY_STORE_PRE_RELEASE_AUDIT_REPORT.md`, `.firebase/` (CLI cache — recommend adding to `.gitignore`)
3. **Future (Steps 7–9, not performed):** EAS credentials check, production AAB build, Play Console upload.
4. **Play Console data-safety + deletion URL:** the in-app Delete Account entry exists (uncommitted change above); Play Console may additionally request a web deletion URL.

---

## Final `git status`

```
 M app/settings.tsx
 M app/terms.tsx
 M components/legal/LegalDocument.tsx
 M constants/config.ts
?? .firebase/
?? FIREBASE_ROOT_PAGE_FIX_REPORT.md
?? PLAY_STORE_PRE_RELEASE_AUDIT_REPORT.md
?? public/index.html
```

All remaining entries are pre-existing, unrelated to Steps 1–6, and were intentionally left uncommitted.

---

## Success Criteria

- [x] API 36 configuration verified/fixed (expo-build-properties, no SDK upgrade)
- [x] App icon square (1024×1024) and Play-appropriate
- [x] Adaptive icon configuration verified (foreground transparent, monochrome white, navy background)
- [x] Monochrome notification icon created + configured
- [x] expo-doctor checked (17/18; remaining issue pre-existing & documented)
- [x] Relevant doctor issues fixed
- [x] `npx tsc --noEmit` passes (0 errors)
- [x] No secrets committed (no .env/keys/keystores in commit)
- [x] Only relevant files committed (9 files)
- [x] Single commit created: `b729de6` "prepare Android app for Play Store" (not pushed)
- [x] EAS credentials NOT touched
- [x] AAB NOT built
- [x] Play Console NOT touched

**STOPPED after Step 6 as instructed.**
