# Execution (Layer 3: Deterministic Tools)

This directory contains deterministic Python scripts that perform specific execution tasks.

## Guidelines
- Scripts should handle API calls, data processing, file operations, and external system interactions.
- Keep tools modular, reliable, testable, fast, and well-commented.
- Read credentials and config from environment variables (`.env`).
